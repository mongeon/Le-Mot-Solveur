@* @foreach(string word in Words.OrderBy(x=>x))
    {
    <div >@word</div><br />
    } *@
<Virtualize Items="@Words">
        <div>@context</div><br />
</Virtualize>
@code {
    [Parameter]
    public IEnumerable<string> Words { get; set; }
}
